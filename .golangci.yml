version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - gocritic
    - gofmt
    - goimports
    - misspell
    - nolintlint

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
